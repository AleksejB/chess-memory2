package configuration.properties

object KeyStorePropertiesConfig {
    const val keyAlias = "keyAlias"
    const val keyPassword = "keyPassword"
    const val keyStoreFile = "storeFile"
    const val keyStorePassword = "storePassword"
}